name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      # SwiftPM enforces the minimum toolchain declared in Package.swift.
      - name: Show Swift toolchain
        run: swift --version
      - name: Swift test
        run: swift test
